OE_GUI viene fatto partire al boot tramite systemctl.

1) copiare file oe_gui.service in /etc/systemd/system/
	(operazione eseguita dallo script install_chroot.sh)


2) systemctl enable oe_gui
	(operazione eseguita dallo script script_config.sh)

Altre funzioni:
systemctl daemon-reload
systemctl start oe_gui
systemctl status oe_gui


CREAZIONE PACKAGE OEDD
--------------------------------------

#Solo la prima volta per la creazione delle dir
dh_make --native --single --packagename oedd_0.0.1 --email massimiliano.negretti@gmail.com

#Ogni volta che si genera una nuova versione
dpkg-buildpackage -us -uc --host-arch armhf
